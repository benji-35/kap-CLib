/*
** KAP PROJECT, 2023
** C Lib
** File description:
** yaml_read
*/

#include "kapparser.h"

void yaml_read(yaml_f *yaml_file) {
    // if (yaml_file == NULL)
    //     return;
    // if (yaml_file->file_content == NULL)
    //     return;
    // list_t *keys = list_create();
    // foreach_l(yaml_file->file_content, node) {
    //     string line = node->data;
    //     if (str_start_with(line, "#"))
    //         continue;
    //     ksize_t empties = number_of_empty(line);
    //     //check_line(line, keys, empties, yaml_file);
    // }
}
